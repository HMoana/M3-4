// import * as carQuoteService from "../../services/carQuoteService";
// import request from "supertest";
// import app from "../../app";


// describe("GET /carQu", () => {
//     test("should respond with error if array not received", async () => {
//       // Arrange
//       jest.spyOn(carQuoteService, "getCarQuotes").mockImplementation(() => {
//         throw new Error("An error occurred");
//       });
  
//       // Act
//       const response = await request(app).get("/carQuotes");
  
//       // Assert
//       expect(response.statusCode).toBe(500);
//     });
//   });